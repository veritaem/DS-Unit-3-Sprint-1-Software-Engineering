mport random
import itertools
from acme import *

list1=['Awesome','Shiny','Portable', 'Improved', 'Impressive']
list2=['Anvil', 'Catapult' 'Disguise' 'Mousetrap', '???']

names list =[zip(x,list2) for x in itertools.permutations(list1,len(list2))]
rando_int = random.randint(5, 101)
rando_fire = random.randint(0.0, 2.6)

def generate products(Class = Product, count = 30):
    prod_list = []
    = Class(self.name)
    for count in count:
        product
        return prod_list

    

def inventory_report():
    print()
    print()
    print()
    print()


if __name__ == '__main__':
    inventory_report(generate_products())
